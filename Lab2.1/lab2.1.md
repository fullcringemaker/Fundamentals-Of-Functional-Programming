% Лабораторная работа № 3 «Обобщённые классы в Scala»
% 1 апреля 2026 г.
% Дмитрий Трофименко, ИУ9-62Б

# Цель работы
Целью данной работы является приобретение навыков разработки обобщённых классов на языке Scala с 
использованием неявных преобразований типов.

# Индивидуальный вариант
Класс Matrix[T], представляющий неизменяемую квадратную матрицу с элементами типа T, для которой 
реализованы две операции: получение значения элемента на i-й строке в j-м столбце, а также 
транспонирование матрицы. В случае, если T — числовой тип, для Matrix[T] должна быть доступна 
дополнительная операция square, возвращающая результат произведения матрицы на саму себя.

# Реализация

```scala
class Matrix[T](val rows: List[List[T]]) {
  val size: Int = rows.length

  def get(i: Int, j: Int): T = rows(i)(j)

  def transpose: Matrix[T] = {
    var result = List[List[T]]()
    var j = 0
    while (j < size) {
      var newRow = List[T]()
      var i = 0
      while (i < size) {
        newRow = newRow :+ rows(i)(j)
        i += 1
      }
      result = result :+ newRow
      j += 1
    }
    new Matrix[T](result)
  }

  def square(implicit num: Numeric[T]): Matrix[T] = {
    var result = List[List[T]]()
    var i = 0
    while (i < size) {
      var newRow = List[T]()
      var j = 0
      while (j < size) {
        var sum = num.zero
        var k = 0
        while (k < size) {
          sum = num.plus(sum, num.times(rows(i)(k), rows(k)(j)))
          k += 1
        }
        newRow = newRow :+ sum
        j += 1
      }
      result = result :+ newRow
      i += 1
    }
    new Matrix[T](result)
  }

  override def toString: String = {
    var s = ""
    var i = 0
    while (i < size) {
      s += "["
      var j = 0
      while (j < size) {
        s += rows(i)(j).toString
        if (j != size - 1) {
          s += ", "
        }
        j += 1
      }
      s += "]"
      if (i != size - 1) {
        s += "\n"
      }
      i += 1
    }
    s
  }
}

object Main extends App {
  val m1 = new Matrix[Int](
    List(
      List(1, 2),
      List(3, 4)
    )
  )
  println(m1)
  println(m1.get(0, 1))
  println(m1.transpose)
  println(m1.square)
  println()

  val m2 = new Matrix[Double](
    List(
      List(1.0, 2.0),
      List(3.0, 4.0)
    )
  )

  println(m2)
  println(m2.get(1, 0))
  println(m2.transpose)
  println(m2.square)
  println()

  val m3 = new Matrix[String](
    List(
      List("a", "b"),
      List("c", "d")
    )
  )
  println(m3)
  println(m3.get(1, 1))
  println(m3.transpose)
  // println(m3.square)
}
```

# Тестирование

Результат запуска программы:

```shell
[1, 2]
[3, 4]
2
[1, 3]
[2, 4]
[7, 10]
[15, 22]

[1.0, 2.0]
[3.0, 4.0]
3.0
[1.0, 3.0]
[2.0, 4.0]
[7.0, 10.0]
[15.0, 22.0]

[a, b]
[c, d]
d
[a, c]
[b, d]
```

# Вывод
В данной лабораторной работе была разработана обобщённая модель квадратной матрицы, позволяющая 
обращаться к элементам по индексам и получать транспонированный вариант. По смыслу задания требовалось 
реализовать базовые операции, которые должны работать для любого типа элементов, а также предусмотреть 
дополнительное действие, доступное только тогда, когда для типа определены числовые операции.

В ходе выполнения работы было показано, как поведение и набор доступных действий можно связать с 
типовым параметром через неявно передаваемые описания операций. Это позволило сделать так, чтобы 
умножение матрицы на саму себя применялось только для числовых типов и опиралось на универсальные 
операции сложения, умножения и нулевого элемента, не привязываясь к конкретному виду чисел.

В результате получено решение, которое корректно обрабатывает матрицы с элементами разных типов и 
демонстрирует различие возможностей в зависимости от этих типов. Тестирование подтверждает 
правильность чтения элементов, корректность транспонирования и вычисления произведения для числовых 
матриц, а также отсутствие возможности выполнить умножение на себя для нечисловых данных.
